pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Building script"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy app'
            }
        }
    }
}
